import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.juggernaut.billboardinspector',
  appName: 'Juggernaut_ITER_Billboard Inspector',
  webDir: 'dist'
};

export default config;
